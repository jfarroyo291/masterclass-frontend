name: Contactame Frontend TIC CI CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  ci:
    runs-on: ubuntu-latest
    container:
      image: node
    steps:
      - uses: actions/checkout@v1
      - name: Install and test
        run: |
          npm install
  cd:
    runs-on: ubuntu-latest
    needs: ci
    steps:
      - uses: actions/checkout@v1
      - name: Docker login
        run: docker login -u ${{ secrets.DOCKER_USER }} -p ${{ secrets.DOCKER_PASS }}
      - name: Docker build
        run: docker build -t contactame-front .
      - name: Tags
        run: |
          docker tag contactame-front ${{ secrets.DOCKER_USER }}/contactame-front:${{ github.sha }}
          docker tag contactame-front ${{ secrets.DOCKER_USER }}/contactame-front:latest
      - name: Push
        run: |
          docker push ${{ secrets.DOCKER_USER }}/contactame-front:${{ github.sha }}
          docker push ${{ secrets.DOCKER_USER }}/contactame-front:latest